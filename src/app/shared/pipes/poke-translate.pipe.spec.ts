import {PokeTranslatePipe} from './poke-translate.pipe';

describe('PokeTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new PokeTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
